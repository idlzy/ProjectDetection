_base_: fcos3d_exp_pgda.yaml

experiment:
  name: fcos3d_kitti_pretrain_local_pgda

data:
  data_root: /home/gy-zb-a-luziyang/datasets/kitti_mw3d
  ready_root: /home/gy-zb-a-luziyang/datasets/kitti_mw3d

train:
  # Keep the same 12-class head as MW3D so this checkpoint can be loaded
  # directly through train.pretrain during the subsequent MW3D fine-tuning.
  pretrain: null
  resume: null
